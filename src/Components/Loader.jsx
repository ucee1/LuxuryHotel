import React from 'react';
import './Loader.css'; // Import custom CSS for loader styling

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
